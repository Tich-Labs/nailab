<div class="grid md:grid-cols-3 gap-10">
  <% pricing_tiers.each do |tier| %>
    <% highlight = tier.fetch(:highlight, false) %>
    <div class="flex flex-col rounded-2xl shadow-lg border-2 <%= highlight ? 'border-purple-600 bg-purple-50 scale-105 z-10' : 'border-gray-200 bg-white' %> p-8 relative">
      <% if highlight %>
        <span class="absolute -top-4 left-1/2 -translate-x-1/2 bg-purple-600 text-white text-xs font-bold px-4 py-1 rounded-full shadow">Most Popular</span>
      <% end %>
      <h2 class="text-2xl font-bold text-gray-900 mb-2 text-center"><%= tier[:name] %></h2>
      <div class="text-4xl font-extrabold text-purple-600 mb-2 text-center"><%= tier[:price] %></div>
      <p class="text-gray-700 mb-6 text-center"><%= tier[:description] %></p>
      <ul class="mb-8 space-y-3">
        <% Array.wrap(tier[:features]).each do |feature| %>
          <li class="flex items-center space-x-2">
            <svg class="w-5 h-5 text-teal-500 flex-shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7"/></svg>
            <span class="text-gray-800"><%= feature %></span>
          </li>
        <% end %>
      </ul>
      <a href="<%= tier[:cta_link] || '/signup' %>" class="mt-auto block w-full text-center px-6 py-3 rounded-lg font-semibold transition-colors shadow-lg hover:shadow-xl <%= highlight ? 'bg-purple-600 text-white hover:bg-purple-700' : 'bg-teal-600 text-white hover:bg-teal-700' %>">
        <%= tier[:cta] || 'Choose Plan' %>
      </a>
    </div>
  <% end %>
</div>
