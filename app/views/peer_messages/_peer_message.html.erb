<div id="peer_message_<%= message.id %>" class="flex <%= message.sender_id == current_user.id ? 'justify-end' : 'justify-start' %>">
  <div class="max-w-xs lg:max-w-md xl:max-w-lg p-4 rounded-lg <%= message.sender_id == current_user.id ? 'bg-nailab-teal text-white' : 'bg-base-200 text-base-content' %>">
    <div class="text-xs <%= message.sender_id == current_user.id ? 'text-white/70' : 'text-base-content/60' %> mb-1">
      <strong><%= message.sender_id == current_user.id ? 'You' : (message.sender.try(:name) || 'Peer') %></strong> • <%= message.created_at.strftime('%b %d, %I:%M %p') %>
    </div>
    <div class="text-sm"><%= simple_format(message.content) %></div>
  </div>
</div>
