<div class="min-h-screen bg-gray-50">
  <div class="max-w-xl mx-auto px-4 py-12">
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-8">
      <h1 class="text-2xl font-bold text-gray-900">Check your email</h1>
      <p class="mt-2 text-gray-600">
        We’ve sent a confirmation link to
        <span class="font-semibold text-gray-900"><%= @email.presence || 'your email' %></span>.
        Click the link to confirm and activate your account.
      </p>
      <div class="mt-6 p-4 bg-slate-50 rounded-2xl border border-slate-200">
        <p class="text-sm text-slate-700">Didn’t receive the email?</p>
        <ul class="mt-2 text-sm text-slate-600 list-disc pl-5 space-y-1">
          <li>Check your spam/junk folder</li>
          <li>Make sure the address is correct</li>
        </ul>
      </div>
      <div class="mt-6">
        <%= form_with url: onboarding_resend_confirmation_path, method: :post, local: true do %>
          <%= hidden_field_tag :email, @email %>
          <%= submit_tag "Resend confirmation email", class: "w-full px-6 py-3 bg-purple-600 text-white rounded-2xl hover:bg-purple-700 transition-colors font-semibold" %>
        <% end %>
      </div>
      <div class="mt-6 text-center">
        <%= link_to "Back to home", root_path, class: "text-sm text-purple-600 hover:text-purple-700" %>
      </div>
    </div>
  </div>
</div>
