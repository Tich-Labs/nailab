# frozen_string_literal: true

# This is a copy of the default ActiveAdmin index page template, with a custom footer partial rendered at the end.

# Execute the ActiveAdmin index block in the proper context
active_admin_config.get_page_presenter(:index).block.call(self)

# Render the shared custom footer after the index table for all resources
div do
  render partial: 'admin/shared/footer', locals: { collection: assigns[:collection] }
end
