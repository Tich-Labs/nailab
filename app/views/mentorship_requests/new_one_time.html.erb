<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-2xl mx-auto">
    <div class="bg-white rounded-lg shadow-md p-8">
      <div class="mb-6">
        <h1 class="text-2xl font-bold text-gray-900 mb-2">Request One-Time Mentorship Session</h1>
        <p class="text-gray-600">Get targeted advice for your specific startup challenge</p>
      </div>
      <%= form_with model: @mentorship_request, url: mentorship_requests_path, method: :post, class: "space-y-6" do |f| %>
        <%= f.hidden_field :request_type, value: "one_time" %>
        <div>
          <%= f.label :topic, class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= f.text_field :topic,
                           placeholder: "e.g., Product-Market Fit, Fundraising Strategy, Team Building",
                           class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-nailab-purple focus:border-nailab-purple",
                           required: true %>
        </div>
        <div>
          <%= f.label :date, class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= f.date_field :date,
                           min: Date.today,
                           class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-nailab-purple focus:border-nailab-purple",
                           required: true %>
          <p class="mt-1 text-sm text-gray-500">Select your preferred date for the session</p>
        </div>
        <div>
          <%= f.label :goal, class: "block text-sm font-medium text-gray-700 mb-2" %>
          <%= f.text_area :goal,
                          rows: 4,
                          placeholder: "Describe what you hope to achieve from this mentorship session. What specific outcomes are you looking for?",
                          class: "w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm focus:ring-nailab-purple focus:border-nailab-purple",
                          required: true,
                          maxlength: 1000 %>
          <p class="mt-1 text-sm text-gray-500">Maximum 1000 characters</p>
        </div>
        <div class="flex space-x-4">
          <%= link_to "← Back to Options",
                      mentorship_select_type_path,
                      class: "flex-1 bg-gray-200 text-gray-700 py-3 px-4 rounded-md hover:bg-gray-300 transition-colors font-medium text-center" %>
          <%= f.submit "Submit Request",
                       class: "flex-1 bg-nailab-purple text-white py-3 px-4 rounded-md hover:bg-nailab-purple-dark transition-colors font-medium" %>
        </div>
      <% end %>
    </div>
    <div class="mt-6 bg-blue-50 border border-blue-200 rounded-md p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-blue-800">What happens next?</h3>
          <div class="mt-2 text-sm text-blue-700">
            <p>After submitting, our team will review your request and match you with an appropriate mentor. You'll receive an email confirmation within 2-3 business days.</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
